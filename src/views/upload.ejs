<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>File Upload Result</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 flex justify-center items-center h-screen">

  <div class="bg-gray-700 shadow-2xl rounded-2xl p-8 max-w-md w-full text-center">
    
    <% if (success) { %>
      <!-- ✅ Success Alert -->
      <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-6">
        <strong class="font-bold text-lg">Upload Successful!</strong>
        <span class="block mt-1">Your file has been uploaded successfully.</span>
      </div>

      <!-- Uploaded Image Preview -->
      <div class="mb-6">
        <img src="<%= path %>" alt="Uploaded File" class="mx-auto rounded-lg border shadow-md max-h-60 object-contain" />
        <p class="mt-4 text-gray-100 font-medium">Filename: <span class="text-blue-600"><%= filename %></span></p>
      </div>

      <!-- Buttons -->
      <div class="flex justify-center gap-4">
        <a href="/" class="px-5 py-2 bg-blue-600 text-white rounded-lg shadow hover:bg-blue-700 transition">Upload Another</a>
        <a href="/" class="px-5 py-2 bg-gray-300 text-gray-800 rounded-lg shadow hover:bg-gray-400 transition">Go Home</a>
      </div>

    <% } else { %>
      <!-- ❌ Failure Alert -->
      <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-6">
        <strong class="font-bold text-lg">Upload Failed!</strong>
        <span class="block mt-1"><%= msg %></span>
      </div>

      <!-- Button to go back -->
      <a href="/home" class="px-5 py-2 bg-gray-600 text-white rounded-lg shadow hover:bg-gray-700 transition">Go Back</a>
    <% } %>

  </div>

</body>
</html>
